/* 다음 배열에서 400, 500을 삭제하는 code를 입력하세요.
	let nums = [100, 200, 300, 400, 500]; */

let nums = [100, 200, 300, 400, 500];

// initial answer
nums.pop().pop();

/* pop의 반환값이 원 배열이 아닌 것을 잊지말자! */

// answer
nums.pop();
nums.pop();